# ~/.goalx/config.yaml — GoalX global configuration
#
# This file configures goalx for all projects on this machine.
# Copy to ~/.goalx/config.yaml and edit.

# Engine presets
# Uncomment to override built-in defaults.
# engines:
#   claude-code:
#     command: "claude --model {model_id} --permission-mode bypassPermissions"
#     models:
#       opus: claude-opus-4-6
#       sonnet: claude-sonnet-4-6

# Custom presets (extend built-in: claude, claude-h, codex, mixed)
# presets:
#   my-research:
#     master: {engine: codex, model: gpt-5.4}
#     research: {engine: claude-code, model: opus}
#   my-dev:
#     master: {engine: codex, model: gpt-5.4}
#     develop: {engine: codex, model: gpt-5.4}

# Custom goal dimensions (extend built-in: depth, breadth, creative, feasibility, adversarial, evidence, comparative, user)
# dimensions:
#   architecture: "Architecture focus: evaluate system boundaries, coupling, cohesion, and extensibility"
#   cost: "Cost analysis: estimate compute, API, infrastructure, and maintenance costs for each approach"

# Local execution defaults
# master:
#   engine: codex
#   model: gpt-5.4
# routing:
#   profiles:
#     build_fast: {engine: codex, model: gpt-5.4-mini, effort: minimal}
#   rules:
#     - role: develop
#       efforts: [minimal, low]
#       profile: build_fast
# preferences:
#   develop:
#     guidance: "Use fast for simple fixes."
# local_validation:
#   command: "go build ./... && go test ./... -count=1 && go vet ./..."
